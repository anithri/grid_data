require "spec_helper"

describe GridData::Paginator do

  context "when no paginator can be determined by test" do

    context "when no paginator given for model or by test" do
      it "return the default paginator" do
        pending
      end
    end

    context "when no paginator given for model or by test" do
      it "return the model paginator" do
        pending
      end
    end
  end

end